<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local Flask is HTTP until the site is hosted on HTTPS. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
